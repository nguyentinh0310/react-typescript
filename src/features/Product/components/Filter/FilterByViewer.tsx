import React from 'react';

export interface  FilterByViewerProps {
}

export default function FilterByViewer (props:  FilterByViewerProps) {
  return (
    <div>
      FilterByViewer
    </div>
  );
}
