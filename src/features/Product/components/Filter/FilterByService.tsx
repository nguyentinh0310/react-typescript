import React from 'react';

export interface  FilterByServiceProps {
}

export default function FilterByService (props:  FilterByServiceProps) {
  return (
    <div>
      FilterByService
    </div>
  );
}
